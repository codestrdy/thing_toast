name: thing_toast
description: "Package for Fancy Gradient Toast."
version: 0.3.0
repository: https://github.com/codestrdy/thing_toast

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
